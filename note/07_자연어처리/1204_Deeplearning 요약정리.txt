• 인공지능학습(dl/ml) : 지도학습 vs 비지도학습 vs 강화학습

• DNN/RNN/LSTM/자연어처리

• 스케일 조정(정규화, 표준화)하는 목적? 
	- 모델 계산의 안정성을 향상시키기 위하여

• model = Sequential([]) : 순차적 레이어 모델 생성
  model = Model(inputs= , outputs= )  : 병렬모델
	
	이중분류의 출력층 : Dense(1, activation='sigmoid')
	다중분류의 출력층 : Dense(n, activation='softmax')

• 학습과정 설정
	- 손실함수
		categorical_crossentropy
		sparse_categorical_crossentropy
	- 평가지표 : accuracy, precision, recall
	- accuracy 높이기 위한 방법들