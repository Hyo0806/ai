$('선택자').함수();
1. html() : 해당 선택자에 html코드 삽입, 가져오기
2. text() : 해당선택자에 text를 삽입, 가져오기
3. css('css속성','값')
4. val() : 해당선택자 input box에 value값을 할당, 가져오기
5. 이벤트관련함수
	click(콜백함수)
	keyup(콜백함수)
6. aJax관련함수
	$.ajax()
	$.get()